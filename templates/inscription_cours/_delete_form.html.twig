<form method="post" action="{{ path('app_inscription_cours_delete', {'id': inscription_cour.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ inscription_cour.id) }}">
    <button class="btn">Delete</button>
</form>
